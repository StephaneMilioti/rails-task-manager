<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <h2><input type="checkbox" id="scales" name="completed"
         <%= task.completed ? "checked" : "" %>>
  <%= link_to task.title, task_path(task)%></h2>
<% end %>
<%= link_to "+ Create a new task", new_task_path %>
